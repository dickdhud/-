# 🚀 راهنمای انتشار رایگان وب‌سایت «آتلیه نُوار» در اینترنت

هزینه کل کار: **صفر تومان** — فقط نیاز به یک ایمیل (جیمیل) دارید.
زمان تقریبی: **۲۰ دقیقه**

---

## مرحله ۱: ساخت اکانت گیت‌هاب (۵ دقیقه)

1. وارد سایت [github.com](https://github.com) شوید
   - اگر باز نشد یا کند بود، اول [shekan.ir](https://shekan.ir) (شکن — بازکننده رایگان) را فعال کنید و دوباره بزنید
2. روی **Sign up** کلیک کنید
3. این موارد را به ترتیب وارد کنید:
   - **Email**: ایمیل‌تان
   - رمز عبور (قوی، ترکیب حرف و عدد)
   - **Username**: یک نام کاربری انتخاب کنید — مثلاً `noir-shop` یا `atelier-noir`
   > ⚠️ این یوزرنیم بعداً توی آدرس سایت شما می‌آید: `username.github.io`
4. حیوان/پازل امنیتی را حل کنید و کد تأییدی که برای ایمیل‌تان آمده را وارد کنید
5. اکانت شما آماده است ✅

---

## مرحله ۲: ساخت مخزن (Repository) سایت

1. بالای صفحه روی **+** کلیک کنید ← **New repository**
2. در فیلد **Repository name** بنویسید: `noir`
3. گزینه **Public** را بزنید
4. تیتی چیز دیگری لازم نیست ← **Create repository**

> حالا آدرس سایت شما این خواهد بود:
> `https://USERNAME.github.io/noir`
> (به‌جای USERNAME یوزرنیمی که ساختید)

---

## مرحله ۳: آپلود فایل‌های سایت

1. فایل **noir-site.zip** را که کنار این راهنما گذاشته‌ام، دانلود و از حالت فشرده خارج کنید (Extract)
2. داخل صفحه مخزن، روی **uploading an existing file** (یا **Add file → Upload files**) کلیک کنید
3. **همه‌ی محتوای داخل پوشه را انتخاب و درگ کنید** داخل صفحه گیت‌هاب:
   ```
   index.html
   robots.txt
   sitemap.xml
   css/
   js/
   assets/
   fonts/
   ```
   > ⚠️ حتماً خودِ فایل‌ها و پوشه‌ها را درگ کنید، نه یک پوشه‌ی مادر که همه را داخلش دارد. فایل `index.html` باید در ریشه‌ی مخزن باشد.
4. پایین صفحه روی **Commit changes** سبز بزنید

---

## مرحله ۴: روشن کردن GitHub Pages

1. داخل مخزن، تب **Settings** (تنظیمات) ← منوی سمت چپ **Pages**
2. بخش **Build and deployment**:
   - **Source**: `Deploy from a branch`
   - **Branch**: `main` و پوشه `/ (root)` ← **Save**
3. حدود ۱ تا ۳ دقیقه صبر کنید
4. همین صفحه را رفرش کنید — بالای آن نوشته می‌شود:
   > Your site is live at `https://USERNAME.github.io/noir`

🎉 **سایت شما روی اینترنت است!** لینک را برای هرکس بفرستید، باز می‌شود.

---

## مرحله ۵: نهایی کردن سئو (با کمک من)

وقتی لینک زنده را داشتید، **نام کاربری یا لینک سایت را به من بدهید** تا:

1. آدرس واقعی را داخل `robots.txt`، `sitemap.xml` و تگ‌های `og:` بگذارم
2. نسخه نهایی را به شما بدهم → فقط همین ۳ فایل را روی گیت‌هاب جایگزین می‌کنید (روی هر فایل کلیک ← آیکون قلم ← جایگذاری ← Commit)

---

## مرحله ۶: معرفی به گوگل (Search Console)

1. وارد [Google Search Console](https://search.google.com/search-console) شوید (شکن را همچنان فعال نگه دارید)
2. **Start now** ← لاگین با جیمیل
3. گزینه‌ی **URL prefix** ← آدرس سایت‌تان را بنویسید: `https://USERNAME.github.io/noir/`
4. روش تأیید **HTML tag** را انتخاب کنید → یک تگ `<meta ...>` به شما می‌دهد
5. **آن تگ را برای من بفرستید** تا داخل سایت‌تان بگذارمش
6. فایل تغییریافته را روی گیت‌هاب جایگزین کنید ← در Search Console دکمه‌ی **Verify**
7. از منوی چپ **Sitemaps** ← بنویسید: `sitemap.xml` ← **Submit**
8. از کادر بالای صفحه (URL Inspection) آدرس سایت را بزنید ← **Request indexing**

تمام! معمولاً بین **۲ روز تا ۲ هفته** طول می‌کشد که سایت‌تان در نتایج گوگل دیده شود.

---

## 🔄 از این به بعد چطور سایت را به‌روز کنم؟

- **تغییر محصول از پنل مدیریت خودم** → نیازی به هیچ کاری نیست (خودکار ذخیره می‌شود)
- **تغییر فایل‌ها با کمک من** → فایل تغییریافته را روی گیت‌هاب جایگزین می‌کنید (Add file ← همانند مرحله ۳)
- بعداً هر زمان خواستید دامنه‌ی اختصاصی `.ir` بخرید (حدود ۱۰۰ هزار تومان در سال)، فقط کافیست بهم بگویید تا همان‌ها را به هاست رایگان متصل کنم

---

## ❓ مشکل داشتید؟

هر مرحله که گیر کردید، اسکرین‌شات یا توضیح مشکل را بفرستید تا راهنمایی‌تان کنم.
