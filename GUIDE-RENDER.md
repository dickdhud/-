# 🚀 راهنمای انتشار نسخه‌ی واقعی (با بک‌اند) روی Render — رایگان

این نسخه علاوه بر فروشگاه، **سرور واقعی** هم دارد؛ یعنی:
- محصولاتی که در پنل می‌سازید برای **همه بازدیدکننده‌ها** مشترک است
- سفارش‌های مشتری‌ها واقعاً روی سرور ثبت می‌شود
- رمز ادمین امن است (فقط سمت سرور چک می‌شود)

> ℹ️ چون سرور Node داریم، GitHub Pages کافی نیست؛ از **Render.com** رایگان استفاده می‌کنیم.

---

## پیش‌نیاز (همون مراحل گیت‌هاب)

مراحل ۱ تا ۳ راهنمای قبلی (`DEPLOY-GUIDE.md`) را انجام داده باشید:

1. ✅ اکانت گیت‌هاب دارید
2. ✅ مخزنی (مثلاً `noir`) دارید
3. ✅ **پکیج جدید `noir-site.zip` را دانلود و محتوایش را روی مخزن آپلود کنید** — این پکیج جدید به‌علاوه‌ی این فایل‌هاست: `server.js` · `package.json` · `js/data.js` · `.gitignore`

> ⚠️ اگر قبلاً آپلود کرده بودید، دوباره همه‌ی فایل‌ها را جایگزین کنید (Drag & Drop ← Commit).

---

## مرحله ۱: ساخت اکانت Render (۳ دقیقه)

1. وارد [render.com](https://render.com) شوید
   - اگر صفحه باز نشد یا کند بود، از [شکن](https://shekan.ir) یا فیلترشکن استفاده کنید
2. **Get Started** ← ساده‌ترین راه: **Sign up with GitHub** (با همان اکانت گیت‌هابی که ساختید)
3. ایمیل را تأیید کنید

---

## مرحله ۲: اتصال سایت (۵ دقیقه)

1. داشبورد Render ← دکمه‌ی **New +** ← **Web Service**
2. بخش **Connect a repository** ← گیت‌هاب را Connect کنید و مخزن `noir` را انتخاب کنید
3. در فرم تنظیمات:
   - **Name**: `noir-shop` ← این می‌شود آدرس سایتتان: `noir-shop.onrender.com`
   - **Region**: `Frankfurt, EU (Central)` (نزدیک‌ترین به ایران)
   - **Branch**: `main`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`  ← خودش معمولاً این را تشخیص می‌دهد
   - **Instance Type**: **Free** ✅
4. پایین صفحه، بخش **Environment Variables** ← **Add Environment Variable**:
   - **`ADMIN_PASS`** = `یک-رمز-سخت-و-طولانی-بنویسید`
   > 🔑 این همان رمز پنل مدیریت شماست — از این طریق رمز دیگر هیچ‌جای کد سایت دیده نمی‌شود.
5. روی **Deploy Web Service** بزنید

---

## مرحله ۳: تماشای دیپلوی (۲ دقیقه)

1. به تب **Logs** نگاه کنید؛ وقتی این خط را دیدید یعنی موفق شدید:
   ```
   ✦ آتلیه نُوار روی http://0.0.0.0:... در حال اجراست (API فعال)
   ```
2. بالای صفحه لینک سایت شماست: `https://noir-shop.onrender.com`
3. بازش کنید و تست کنید:
   - خانه‌ی فروشگاه لود می‌شود ✔
   - `noir-shop.onrender.com/#/admin` با رمز جدیدی که برای ADMIN_PASS گذاشتید وارد پنل شوید
   - یک محصول تستی اضافه کنید ← از کامپیوتر/گوشی دیگری باز کنید ← باید همان‌جا هم نمایش داده شود! 🎉

---

## ⚠️ سه نکته‌ی مهم در مورد پلن رایگان Render

### ۱. خواب سرویس
بعد از **۱۵ دقیقه بدون بازدید**، سرویس می‌خوابد و اولین بازدید بعدی حدود **۲۰ تا ۳۰ ثانیه** طول می‌کشد تا بیدار شود. برای مشتری‌های واقعی قابل‌قبول است؛ برای نمایش در مصاحبه/جلسه، اول خودتان یک بار باز کنید.

### ۲. دیتای فایل روی دیسک موقت
روی پلن رایگان، وقتی سرویس **دوباره دیپلوی می‌شود** (مثلاً کد جدید آپلود می‌کنید)، دیتای ذخیره‌شده در همان سرور ریست می‌شود و دیتای اولیه برمی‌گردد.

**راه‌حل ما — از پنل مدیریت استفاده کنید:**
- **⬇ پشتیبان‌گیری از دیتا**: فایل کامل محصولات + سفارش‌ها را دانلود می‌کند
- **⬆ بازیابی نسخه**: بعد از هر دیپلوی مجدد، همان فایل را آپلود کنید تا همه‌چیز برگردد

### ۳. رفع کامل (هر وقت خواستید)
- دیسک پایدار Render (حدود ۱ دلار در ماه) یا دیتابیس ابری رایگان — هر وقت گفتید اضافه‌اش می‌کنم.

---

## مرحله ۴: معرفی به گوگل (مانند قبل)

1. آدرس جدید (`https://noir-shop.onrender.com`) را به من بدهید تا فایل‌های سئو (`robots.txt`, `sitemap.xml`, تگ‌های og) را با همان به‌روز کنم
2. طبق مرحله‌ی ۶ راهنمای قبلی (`DEPLOY-GUIDE.md`)، در Google Search Console ثبت کنید

---

## مرحله ۵ (اختیاری): دامنه‌ی اختصاصی — بعداً

اگر بعداً دامنه‌ی `.ir` خریدید:
1. Render ← Settings ← **Custom Domain** ← دامنه‌تان را وارد کنید
2. یک رکورد `CNAME` در پنل دامنه‌تان (مثل nic.ir) بسازید ← من راهنمایی‌تان می‌کنم
3. SSL رایگان خودکار صادر می‌شود

---

## 🔄 چرخه‌ی به‌روزرسانی سایت بعد از انتشار

| کاری که می‌خواهید بکنید | راهش |
|---|---|
| تغییر محصول/سفارش/قیمت | از پنل مدیریت خود سایت — نیازی به کد نیست ✅ |
| تغییر متن/شماره تماس/روش ارسال | فایل `js/config.js` را در گیت‌هاب ویرایش کنید (روی فایل کلیک ← قلم ← Commit) — Render خودکار مجدد دیپلوی می‌کند |
| تغییر با کمک من | فایل جدید را جایگزین گیت‌هاب کنید |

---

## 💳 فعال‌سازی پرداخت آنلاین واقعی (زرین‌پال)

سایت از ابتدا با درگاه **زرین‌پال** یکپارچه شده. برای واقعی شدن:

1. در [zarinpal.com](https://zarinpal.com) به‌عنوان **پذیرنده (Merchant)** ثبت‌نام کنید و **کد پذیرندگی** (Merchant ID — یک رشته‌ی ۳۶ کاراکتری) را بگیرید
   > ℹ️ دریافت درگاه در ایران معمولاً نیازمند **اینماد** (مجوز کسب‌و‌کار اینترنتی از enamad.ir) است
2. در Render ← سرویس شما ← **Environment** ← دو متغیر اضافه کنید:

   | کلید | مقدار |
   |---|---|
   | `ZP_MERCHANT` | کد پذیرندگی که از زرین‌پال گرفتید |
   | `BASE_URL` | `https://noir-shop.onrender.com` (آدرس واقعی سایت‌تان — بدون / انتهایی) |

3. **Save Changes** ← سرویس خودکار دیپلوی مجدد می‌شود
4. در لاگ‌ها این پیام را باید ببینید: `درگاه پرداخت: فعال (live)`

از این لحظه: مشتری «پرداخت آنلاین» را بزند ← به صفحه دروازه زرین‌پال می‌رود ← بعد از پرداخت خودکار برمی‌گردد به صفحه «ثبت موفق» و وضعیت سفارش در پنل شما **«پرداخت شده»** سبز می‌شود 🎯

> 🧪 برای تمرین بدون پول واقعی: متغیر سوم `ZP_MODE = sandbox` بگذارید و کد تستی sandbox روی zarinpal.com دریافت کنید.

---

## ❓ مشکلی پیش آمد؟

- **سایت بالا نمی‌آید**: در Render تب Logs را چک کنید — خطا را برایم بفرستید
- **رمز کار نمی‌کند**: در Render ← Environment ← مقدار ADMIN_PASS را چک کنید
- **باز و واخوردگی اولیه**: همان خواب سرویس رایگان است؛ یک بار رفرش کنید
